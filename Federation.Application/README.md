# Federation.Application

Implements the `venta/v0.1` server-to-server federation protocol.

**Full protocol specification:** [`docs/federation-protocol.md`](docs/federation-protocol.md) -
wire format, instance lifecycle, the canonical-ID/shadow-entity data model, outbound/inbound
wiring architecture, split-brain handling, delivery reliability, security, and versioning.
